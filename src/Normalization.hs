module Normalization where

import Syntax

intify :: ([Def String], Process String) -> ([Def Int], Process Int)
intify (defs, proc) = error "intity TODO"

--unfoldDefs :: [Def] -> Process -> Process
--unfoldDefs defs proc = error "unfoldDefs TODO"
--
--scopeExtrude :: Process -> Process
--scopeExtrude proc = error "scopeExtrude TODO"
